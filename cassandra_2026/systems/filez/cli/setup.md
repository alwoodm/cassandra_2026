# filez CLI setup
Install dependencies:
   ```bash
   poetry install
   ```

Copy `.env.example` to `.env` in the project root and fill in your values:
   ```bash
   cp .env.example .env
   ```

Run:
   ```bash
   poetry run filez
   ```
